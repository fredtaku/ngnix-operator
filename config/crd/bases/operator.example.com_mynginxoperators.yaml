---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: mynginxoperators.operator.example.com
spec:
  group: operator.example.com
  names:
    kind: MyNginxOperator
    listKind: MyNginxOperatorList
    plural: mynginxoperators
    singular: mynginxoperator
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MyNginxOperator is the Schema for the mynginxoperators API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MyNginxOperatorSpec defines the desired state of MyNginxOperator
            properties:
              forceRedploy:
                description: |-
                  ForceRedploy is any string, modifying this field
                  instructs the Operator to redeploy the Operand
                type: string
              port:
                description: Port is the port number to expose on the Nginx Pod
                format: int32
                type: integer
              replicas:
                description: Replicas is the number of deployment replicas to scale
                format: int32
                type: integer
            required:
            - forceRedploy
            type: object
          status:
            description: MyNginxOperatorStatus defines the observed state of MyNginxOperator
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
