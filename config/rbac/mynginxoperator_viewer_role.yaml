# permissions for end users to view mynginxoperators.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: nginxoperator
    app.kubernetes.io/managed-by: kustomize
  name: mynginxoperator-viewer-role
rules:
- apiGroups:
  - operator.example.com
  resources:
  - mynginxoperators
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - operator.example.com
  resources:
  - mynginxoperators/status
  verbs:
  - get
